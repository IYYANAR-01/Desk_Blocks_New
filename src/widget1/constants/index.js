import { DEFAULT_GLOBAL_CHECKLIST_DATA } from "../../constants";

export const DEFAULT_DATA = {
    default: [...DEFAULT_GLOBAL_CHECKLIST_DATA]
};

export const CONTEXT_NAME = "global_checklist_data";